---
import ApplicationPage from '../../components/application/ApplicationPage.astro';
import { applicationsFor } from '../../data/applications';
export const getStaticPaths = () => applicationsFor('nl').map(application => ({ params: { bedrijf: application.slug }, props: { application } }));
const { application } = Astro.props;
---
<ApplicationPage application={application} />
